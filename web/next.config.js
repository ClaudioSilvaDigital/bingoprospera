/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',     // substitui o "next export"
  // opcional: se usar imagens do Next, desabilite otimização:
  // images: { unoptimized: true },
};
module.exports = nextConfig;
